<Window x:Class="Salamandra.Views.EventWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Salamandra.Views"
        xmlns:converters="clr-namespace:Salamandra.Converters"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="Evento" Height="600" Width="480"
        ResizeMode="NoResize" ShowInTaskbar="False" WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <converters:DayOfWeekStringConverter x:Key="DayOfWeekStringConverter"/>
    </Window.Resources>
    <StackPanel Margin="8">
        <GroupBox Header="Início e Término">
            <StackPanel Margin="4">
                <TextBlock Text="Data e Hora de Início"/>

                <Grid Margin="0 4 0 0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <DatePicker SelectedDateFormat="Short" MinWidth="150" Grid.Column="0"/>
                    <xctk:TimePicker Grid.Column="1" Margin="8 0 0 0" MinWidth="150"/>
                    <CheckBox Grid.Column="2" Content="Imediato"
                              Margin="8 0 0 0" VerticalAlignment="Center"/>
                </Grid>

                <CheckBox Content="Data e Hora de Expiração" Margin="0 8 0 0"/>

                <Grid Margin="0 4 0 0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <DatePicker SelectedDateFormat="Short" MinWidth="150" Grid.Column="0"/>
                    <xctk:TimePicker Grid.Column="1" Margin="8 0 0 0" MinWidth="150"/>
                </Grid>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Horários e Dias" Margin="0 8 0 0">
            <StackPanel Margin="4">
                <CheckBox Content="Outros Horários"/>
                <xctk:CheckListBox Margin="0 4 0 0" ItemsSource="{Binding Hours}"
                                   IsSelectAllActive="True" SelectAllContent="Selecionar Todos">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="4" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <xctk:CheckListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding ., StringFormat='00.##'}"/>
                        </DataTemplate>
                    </xctk:CheckListBox.ItemTemplate>
                </xctk:CheckListBox>
                <CheckBox Content="Dias da Semana" Margin="0 8 0 0"/>
                <xctk:CheckListBox Margin="0 4 0 0" ItemsSource="{Binding Days}"
                                   IsSelectAllActive="True" SelectAllContent="Selecionar Todos">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="7" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <xctk:CheckListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding ., Converter={StaticResource DayOfWeekStringConverter}}"/>
                        </DataTemplate>
                    </xctk:CheckListBox.ItemTemplate>
                </xctk:CheckListBox>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Tipo do Evento" Margin="0 8 0 0">
            <StackPanel Margin="4">
                <ComboBox/>
                <Grid Margin="0 8 0 0">
                    <Grid.ColumnDefinitions >
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0" Height="24"/>
                    <Button Grid.Column="1" Content="..." Width="30" Height="24" Margin="4 0 0 0"/>
                </Grid>
            </StackPanel>
        </GroupBox>
    </StackPanel>
</Window>
